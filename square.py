
def area(a):
    #функция принимает переменную a - сторону квадрата.
    return a * a
    #Функция возвращает площадь квадрата.


def perimeter(a):
    #функция принимает переменную а - сторону квадрата.
    return 4 * a
    #функция возвращает периметр квадрата.

print(area(5))